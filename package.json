{
  "name": "tmux-session",
  "version": "1.0.0",
  "description": "Interactive CLI for managing tmux sessions",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "tmux-session": "./tmux-session"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile tmux-session"
  },
  "keywords": ["tmux", "cli", "session-manager"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^0.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
